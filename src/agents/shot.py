class Shot:
    shot_size: str   # 景别: 大远景（Extreme Long Shot, ELS）全景（Full Shot, FS）中景（Medium Shot, MS）近景（Close-up, CU）特写（Extreme Close-up, ECU）
    shot_prompt: str # 构图描述
    camera_angle: str # 相机角度 摄影机相对于被摄对象的位置，如高角度、低角度、俯拍、仰拍等
    degree: str # 相机角度 例如：45度、60度等
    bg_voice: str # 背景语音 场景中环境的声音，如风声、雨声、雨声等

    
